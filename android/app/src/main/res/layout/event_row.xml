<?xml version="1.0" encoding="utf-8"?>
<!-- One event. Tapping it makes this the night the next capture belongs to. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:minHeight="72dp"
    android:padding="16dp"
    android:background="@drawable/card_background">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/eventName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/tumtum_white" />

        <TextView
            android:id="@+id/eventDetail"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textSize="13sp"
            android:textColor="@color/tumtum_muted" />
    </LinearLayout>

    <!-- Visible only on the chosen event. -->
    <TextView
        android:id="@+id/chosen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:text="@string/chosen_mark"
        android:textSize="18sp"
        android:visibility="gone"
        android:textColor="@color/tumtum_lime" />

    <!--
      Editing is its own explicit door. Tapping the row chooses the night, and
      "não consegui editar o evento" showed that a second, hidden meaning on
      the same tap is unfindable — so the second meaning gets its own word.
    -->
    <TextView
        android:id="@+id/editRow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:minWidth="48dp"
        android:minHeight="48dp"
        android:gravity="center"
        android:text="@string/edit_row"
        android:textSize="13sp"
        android:textColor="@color/tumtum_muted" />
</LinearLayout>
